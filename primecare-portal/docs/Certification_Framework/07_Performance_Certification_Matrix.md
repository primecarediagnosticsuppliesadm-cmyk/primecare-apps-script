# 07 — Performance Certification Matrix

**Surface-level performance budgets, measurement commands, and baselines for release certification.**

Evidence file: `docs/hq-certification/HQ_PERFORMANCE_CERTIFICATION.md` (generated by cert script)

---

## Certification gate

| Metric | Threshold | Environment |
|--------|-----------|-------------|
| **Max bounded read** | ≤ **500 ms** | PERF tenant (100k orders) |
| **Unbounded surfaces** | **0** | All HQ list reads |
| **QA cold navigation** | ≤ **400 ms** | HQ tenant smoke probe |

FAIL if any bounded surface exceeds 500 ms on PERF tenant or unbounded read detected.

---

## Surface matrix

| Surface ID | UI screen | API entry | Bounded | Target (cold) | PERF baseline | QA probe |
|------------|-----------|-----------|---------|---------------|---------------|----------|
| `orders-list` | OrdersPage | `getOrdersRead` | yes (100) | 350 ms | 282 ms | 239 ms |
| `collections` | CollectionsPage | `getCollectionsRead` | yes | 200 ms | 154 ms | 138 ms |
| `admin-dashboard` | DashboardPage | `getAdminDashboardRead` | yes | 350 ms | 165 ms | 273 ms |
| `ops-center` | OperationsCenterAdminPage | ops loader bundle | yes | 400 ms | 366 ms | — |
| `revenue-funnel` | RevenueFunnelPage | orders probe | yes (100) | 300 ms | 266 ms | — |
| `lab-catalog` | LabOrderingPage | `getLabCatalogRead` | yes | 300 ms | — | ~92 ms |
| `lab-recent-orders` | LabOrderingPage | `getLabRecentOrdersRead` | yes | 250 ms | — | ~263 ms |
| `logistics-dashboard` | LogisticsDeliveryPage | logistics KPI read | yes | 400 ms | — | — |
| `efi` | ExecutiveFinancialIntelligencePage | EFI model build | read-only | 400 ms | — | — |
| `labs-list` | LabsPage | `getLabsCredit` | yes | 300 ms | — | — |
| `inventory-stock` | StockPage | `getStockDashboard` | yes | 300 ms | — | 98 ms |
| `qualification` | QualificationReviewPage | `getQualificationReviewRead` | yes | 200 ms | — | 112 ms |

**Baselines:** PERF from `run-hq-performance-certification.mjs` (2026-07-02, tenant `3ced2736-…`); QA from `measure-hq-navigation-perf.mjs`.

---

## Before / after tracking

Record in each release scorecard when perf-impacting changes land:

| Change | Surface | Before | After | Delta | Commit |
|--------|---------|--------|-------|-------|--------|
| Orders RLS probe cap | `orders-list` | unbounded | 500 cap | improved | `3de642b` |
| Item count lookup keys | `orders-list` | ~8s naive | chunked | improved | `3de642b` |
| Search debounce 300ms | `orders-list` UI | immediate | debounced | UI improved | `3de642b` |

---

## Measurement commands

### PERF tenant certification (isolated scale)

```bash
cd primecare-portal

# Use existing seeded tenant (recommended)
PERF_SKIP_SEED=1 PERF_TENANT_ID=<tenant-uuid> node scripts/run-hq-performance-certification.mjs

# Full re-seed (requires service-role seed path — may FAIL on RLS)
node scripts/run-hq-performance-certification.mjs
```

Output: `docs/hq-certification/HQ_PERFORMANCE_CERTIFICATION.md`

### QA tenant navigation probe

```bash
node scripts/measure-hq-navigation-perf.mjs
```

### Ad-hoc single surface timing

Use `perfTime` / `perfLog` in `primecareSupabaseApi.js` with browser network tab or script-level `performance.now()` wrapper.

---

## Largest slow query identification

From PERF cert report:

1. Sort benchmarks by `ms` descending
2. Record **slowest surface** in release scorecard
3. If slowest > 400 ms → investigate:
   - Missing index (`idx_orders_tenant_order_date`, `idx_payments_tenant_payment_date`)
   - Unbounded join (violation — fix immediately)
   - Parallel read fan-out (Operations Center)

**Current slowest (PERF):** Operations Center loader — **366 ms** (within gate)

---

## Recommendations catalog

| Priority | Recommendation | Surfaces |
|----------|----------------|----------|
| P1 | Never remove `hqReadBounds.js` limits | all list surfaces |
| P1 | Keep PERF tenant isolated from QA writes | perf cert |
| P2 | Lazy-load Operations Center sub-panels | `ops-center` |
| P2 | Fix PERF seed RLS for tenant refresh | infrastructure |
| P3 | Code-split `index-*.js` > 500 kB | initial load |
| P3 | Add lab logistics read to perf probe | `logistics-dashboard` |

---

## Performance cert bundles

| Release type | Required |
|--------------|----------|
| O2C release | PERF cert + `measure-hq-navigation-perf` |
| UI-only change | QA probe on affected surface |
| Read-path change | PERF cert + `verify-bounded-reads` |
| Schema index add | PERF cert before/after |

---

## WARN handling

| WARN | Action |
|------|--------|
| Surface 400–500 ms | Document in scorecard; plan optimization |
| Surface > 500 ms | **FAIL** — block release |
| PERF seed RLS fail | Use existing PERF tenant; log GAP |
| Bundle > 500 kB | WARN only — not a cert blocker |

---

## Related scripts

| Script | Purpose |
|--------|---------|
| `run-hq-performance-certification.mjs` | PERF tenant benchmarks |
| `measure-hq-navigation-perf.mjs` | QA multi-role probe |
| `verify-bounded-reads.mjs` | Static + runtime bounded guards |
| `verify-perf-scale-counts.mjs` | PERF tenant row counts |
